# 从网页挂件到桌面宠物：小鲸鱼移植全流程教程

> 存档目的：把「用浏览器壳把网页挂件移植成桌面宠物」的完整过程、方案选型和踩坑记录下来，
> 之后可以照着这份教程教别人做同类事情（桌面宠物 / 透明悬浮窗 / 点击穿透挂件）。
> 对应项目：`D:\study\whale-desktop\`，素材来源：[MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget)

---

## 0. 成果预览

- 鲸鱼悬浮在桌面右下角，**只有鲸鱼本体、菜单、气泡能拦截鼠标，其余全部点击穿透直达桌面**
- 原挂件功能 100% 保留：余额滚动、今日已用记账、随机台词、音效、拖拽吸附、镜像翻转、Q弹、菜单缩放
- **前端一行没改、后端零逻辑重写**——全部靠「壳」的适配完成
- 托盘：穿透开关 / 开机自启 / 刷新 / 退出
- **一键安装包**：electron-builder + NSIS 打成 `DeepSeekWhale Setup 1.0.0.exe`，双击装完即用（见 §6）

## 1. 需求拆解与方案选型

需求三句话拆成三个技术点：

| 需求 | 技术手段 | 备选 | 为什么选它 |
|------|----------|------|-----------|
| 浏览器壳（不重写前端） | Electron 透明窗口加载本地页面 | Tauri（要装 Rust 工具链）/ WebView2（要写 C++/C# 宿主） | Node 环境现成，JS 全栈，社区方案最多 |
| 抠掉背景只留鲸鱼 | 页面 html/body 透明 + BrowserWindow `transparent:true` | CSS 滤镜抠图（脆弱，改前端） | 鲸鱼 PNG 本身是 cut-out，纯透明即可，零改动 |
| 透明区不挡桌面 | `setIgnoreMouseEvents(true, {forward:true})` + 渲染进程实时命中检测 | 窗口缩小到鲸鱼大小（牺牲拖拽/吸附功能） | 全屏透明窗口保留原挂件全部交互，穿透按像素判定 |

**关键决策**：窗口做成「铺满主屏工作区」的透明置顶层，而不是鲸鱼大小的窗口。
理由：原挂件的拖拽/四边吸附是它的核心功能，但它的坐标模型是「在窗口内移动」——
窗口若只有鲸鱼大，拖拽就废了；窗口铺满桌面，鲸鱼就能拖到桌面任意位置且吸附屏幕边缘。

## 2. 核心思想：前端零改写 + 后端 shim

### 2.1 先读懂原插件长什么样

读源码第一件事是画依赖图。这个插件是 DSH（DeepSeek Harness）的 bundle 插件，结构是：

```
lib/index.js（2000 行）
├── 前半段：WIDGET_JS 模板字符串 = 完整前端代码（自绘 DOM，不依赖任何框架）
│    └── 依赖 8 个同源接口：/dsh-whale/{image.png, rua.gif, balance.json,
│        size.json, last-turn.json, sound/press.mp3, sound/release.mp3, widget.js}
└── 后半段：apply(ctx) 宿主侧插件，用依赖注入拿服务：
     ctx.webServer.register(...)   ← 注册上面 8 个路由
     ctx.credentials.resolve(...)  ← 拿 API Key
     ctx.on('session/event', ...)  ← 会话事件（每轮消耗统计）
     ctx.webServer.tapIndex(...)   ← 往 DSH 网页 HTML 里插 <script>
```

**这是移植能否「零改写」的关键判断**：插件边界清晰、依赖全部走 `apply(ctx)` 注入，
而不是硬依赖 DSH 全局变量 → 可以做一个假的 ctx 骗过它。

### 2.2 shim ctx：40 行适配器复用全部后端

```js
// 我们的 main.js 里：
const routes = []  // 接住插件注册的路由
const ctx = {
  webServer: {
    register(route) { routes.push(route); return () => {} },
    tapIndex() { return () => {} }        // script 注入由我们自己的页面负责
  },
  credentials: {
    async resolve(name) {                  // 每次热读 config.json，改 key 不用重启
      const cfg = JSON.parse(fs.readFileSync(CONFIG_FILE, 'utf8'))
      return (cfg && cfg[name]) || process.env[name] ? { value: String(cfg[name]) } : null
    }
  },
  on() { return () => {} },                // 桌面版无 DSH 会话 → 空监听
  effect(fn) { cleanupFns.push(fn) }       // 退出时清理
}
process.env.DSH_HOME = DATA_DIR           // 劫持：插件的配置文件落到我们的 data/ 目录
const plugin = await import('file:///D:/study/DeepSeek-Balance-Whale-Widget/lib/index.js')
plugin.apply(ctx)                          // 8 个路由全部注册完成，原样生效

// 分发请求：pathname 精确匹配路由表
http.createServer((req, res) => {
  const pathname = new URL(req.url, 'http://x').pathname
  const route = routes.find(r => r.kind === 'exact' && r.path === pathname)
  route ? route.handler(req, res) : res.writeHead(404).end()
})
```

**教学点**：这就是依赖注入的价值——插件只认接口不认实现，换个宿主只需换 ctx。
同类技巧适用于任何「为 A 平台写的插件想跑在 B 平台」的场景。
好处：原插件更新（git pull）后桌面端自动同步，零维护成本。

### 2.3 前端零改写：原样吐出

- 页面 `index.html` 只有透明样式 + 一行 `<script defer src="/dsh-whale/widget.js">`
- `/dsh-whale/widget.js` 是插件自己注册的路由，`res.end(WIDGET_JS)` 原样吐出——**模板字符串的
  反斜杠转义由 Node 的模板解析器天然处理**，我们一行解包代码都不用写
- 安全检查：移植前扫描模板字符串内有无 `${}` 插值和内部反引号（本插件都没有 → 可安全当纯文本用）

## 3. 分步实操

### 3.1 克隆素材（GitHub 被墙的处理）

```bash
git clone --depth 1 git@github.com:MeteorNOX/DeepSeek-Balance-Whale-Widget.git   # SSH 方式
# HTTPS 直连超时（国内常见），SSH 走 22 端口通常可用；已配好 712sir 账号
```

### 3.2 初始化 Electron 工程

```powershell
mkdir whale-desktop; cd whale-desktop
# .npmrc 配 npmmirror 镜像——electron 二进制默认从 GitHub 下载，国内必配：
#   electron_mirror=https://npmmirror.com/mirrors/electron/
#   registry=https://registry.npmmirror.com/
npm install --save-dev electron
npm install --save-dev electron-builder   # 打包用（§6），其工具链镜像见 6.3
```

package.json：`"type": "module"`，`"main": "main.js"`，
**start 脚本带防御**：`"start": "set ELECTRON_RUN_AS_NODE=&& electron ."`（坑 #2）

### 3.3 主进程：透明置顶全屏窗口

```js
const { workArea } = screen.getPrimaryDisplay()
win = new BrowserWindow({
  x: workArea.x, y: workArea.y, width: workArea.width, height: workArea.height,
  transparent: true,        // 关键：窗口透明，页面透明处直接露桌面
  frame: false,             // 无边框
  resizable: false, movable: false, hasShadow: false,
  skipTaskbar: true,        // 不进任务栏（Alt+Tab 也看不到）
  fullscreenable: false,
  webPreferences: { preload: 'preload.cjs', contextIsolation: false, nodeIntegration: false, sandbox: false }
})
win.setAlwaysOnTop(true, 'screen-saver')  // 悬浮在所有窗口之上
win.setIgnoreMouseEvents(true, { forward: true })  // 默认全穿透
await win.loadURL(`http://127.0.0.1:${port}/`)
win.showInactive()          // 不抢当前窗口焦点
```

端口用 `server.listen(0)` 随机分配，避免与别的程序冲突。

### 3.4 抠背景

```html
<style>
  html, body { margin: 0; padding: 0; background: transparent !important; overflow: hidden; }
</style>
```

三层透明缺一不可：**鲸鱼素材透明**（cut-out PNG）→ **页面透明**（上面这段）→ **窗口透明**（transparent:true）。

### 3.5 点击穿透（本教程的核心）

**原理**：`setIgnoreMouseEvents(true, {forward:true})` 的 `forward:true` 是关键——
窗口忽略鼠标，但**鼠标事件仍然转发进页面**（mousemove 照常触发）。
于是渲染进程可以实时判定「光标在不在该拦截的区域」，动态开关穿透：

```
桌面鼠标移动 → 事件转发进页面 → preload 判定命中区域：
  ① 鲸鱼本体：像素级 alpha 检测（透明像素穿透，实体像素拦截）
  ② 菜单按钮：26×26 小矩形（永远可悬停，否则汉堡菜单永远点不出来）
  ③ 打开的菜单/气泡：矩形判定
命中 → ipc 通知主进程 setIgnoreMouseEvents(false)（事件归窗口）
未命中 → setIgnoreMouseEvents(true)（点击直达桌面）
```

**alpha 像素检测**：把鲸鱼图绘到 offscreen canvas（610×610，与原插件 `isWhaleHit` 同套
映射，含左右镜像翻转），查光标点的 alpha 通道：

```js
const d = hitCanvas.getContext('2d').getImageData(x, y, 1, 1).data
return d[3] > 10   // alpha > 10 视为实体像素
```

**状态联动**：菜单/气泡的开合、镜像翻转都会改变命中区域，用 `MutationObserver`
监听 class 变化后用缓存的光标坐标重算——不是只在 mousemove 时算。

**教学点（易错）**：
- 忘了 `forward:true` → 穿透后页面收不到 mousemove，鲸鱼永远点不中
- 菜单按钮如果也做成「像素级」检测 → opacity:0 的按钮永远无法悬停，鸡生蛋问题
- 命中图未加载完时用「鲸鱼整矩形」兜底，避免启动初期鲸鱼点不到

### 3.6 托盘与开机自启

```js
app.setAppUserModelId('com.whale.desktop')  // Windows 上 setLoginItemSettings 的前置要求
app.setLoginItemSettings({ openAtLogin: v, path: process.execPath, args: [APP_DIR] })
// 写入注册表 HKCU Run：electron.exe + 应用目录，登录后自动出现
```

## 4. 踩坑记录（按时间线）

### 坑 #1：GitHub HTTPS 克隆超时
- **现象**：`Failed to connect to github.com port 443 after 21167 ms`
- **排查**：直连被墙，不是 SSL 证书问题（配 `sslVerify false` 没用）
- **解决**：改用 SSH 克隆（`git@github.com:...`），一次成功
- **教训**：国内环境 GitHub 操作一律 SSH 优先；网页访问超时也是常态，别在 WebFetch 上耗时间

### 坑 #2：ELECTRON_RUN_AS_NODE=1 让 electron 冒充 Node（最隐蔽）
- **现象**：`electron --version` 打印 `v24.20.0`（Node 版本！）；跑应用报
  `SyntaxError: The requested module 'electron' does not provide an export named 'BrowserWindow'`
- **排查**：二进制明明存在、文件都全。最后发现环境变量 `ELECTRON_RUN_AS_NODE=1`
  ——它强制 electron.exe 以纯 Node 身份运行。这个变量不在系统/用户注册表里，只存在于
  当时会话的进程环境（来自 Claude Code 宿主环境）
- **解决**：启动脚本里 `set ELECTRON_RUN_AS_NODE=` 清掉再启动
- **教训**：**Electron 报「electron 模块没有导出 BrowserWindow」先查这个环境变量**
  ——它让一切 Electron 行为伪装成 Node，报错极具误导性（看起来像 ESM 兼容问题）

### 坑 #3：electron 二进制下载失败
- **现象**：electron 的 postinstall 要从 GitHub Releases 下载 ~100MB 二进制，国内超时
- **解决**：`.npmrc` 里 `electron_mirror=https://npmmirror.com/mirrors/electron/`，
  registry 也切 npmmirror（项目级 .npmrc，不影响全局）
- **教训**：装完一定要验证 `electron --version` 打出的是 Electron 版本号（v44.x）而非 Node 版本

### 坑 #4：「幽灵写入」之谜 → 穿透正常工作的实证
- **现象**：第一次测试时，没有任何交互，配置里凭空出现 scale:2.5
- **排查**：逐个排除 curl 批次、加载路径、滚轮监听、定时器……最后用像素测量截图确认
  鲸鱼在截图时已经是 2.5 倍大小，倒推时间线：是我自己在终端里的几次点击被
  `forward:true` 转发进了页面——点中鲸鱼菜单按钮 → 菜单打开 → 点中大小滑块 → 2.5 倍 → 
  拖拽鲸鱼 → endDrag 落盘配置
- **结论**：不是 bug，是功能正常：**穿透转发让鲸鱼和桌面同时都能收到我的点击**
- **教学点**：这是「正向验证点击穿透」的好案例——能用幽灵事件反推事件链路；
  也提醒：挂件开着时，桌面右下角的点击会进鲸鱼，属于预期行为

### 坑 #5：npm 找不到 package.json
- **现象**：`npm start` 报 `ENOENT D:\study\package.json`
- **原因**：shell 工作目录被重置回 D:\study
- **解决**：`npm --prefix D:\study\whale-desktop start`
- **教训**：脚本工具别依赖会话的 cwd，显式传 `--prefix` 或绝对路径

### 坑 #6：验证接口时 curl URL 拼错
- **现象**：`curl "http://127.0.0.1$port/..."` 返回空——$port 变量里已经含主机名
- **教训**：shell 变量拼接前先 echo 确认完整 URL；空响应先打 `-w "%{http_code}"`

### 坑 #7：安装目录名不随 productName 走（打包阶段）
- **现象**：静默安装成功后，在预期的 `%LOCALAPPDATA%\Programs\deepseekwhale\` 下找不到 exe
- **排查**：全盘 find 发现实际装到了 `%LOCALAPPDATA%\Programs\whale-desktop\`
- **原因**：electron-builder 的安装目录名取的是 package.json 的 `name` 字段（whale-desktop），
  不是 `build.productName`（DeepSeekWhale）；productName 只决定 exe 名和开始菜单显示名
- **教训**：验证安装位置别靠猜，`find` 一下再下结论；想让目录名也换就改 `name` 字段

### 坑 #8：NSIS 向导图标只认 .ico（打包阶段）
- **现象**：切到向导模式（`oneClick:false`）并给 `installerIcon` 指了个 PNG 后构建失败：
  `Error while loading icon from "build\icon.png": invalid icon file`
- **原因**：应用图标（win.icon）PNG 可以，electron-builder 会自己转 .ico；
  但 NSIS 向导的 MUI_ICON 是把文件直接传给 makensis，只认真 .ico
- **解决**：把 PNG 转成 .ico（256×256）再指给它——PowerShell 一行即可：
  System.Drawing 加载 PNG → 缩到 256 → GetHicon() → Icon.Save 写 .ico
- **教训**：NSIS 侧的资源（向导图标/安装图标）要 .ico，只有 electron-builder 自己负责的
  资源（app exe 图标）才接受 PNG 自动转换

### 坑 #9：从 git-bash 跑安装器，/S 被偷偷转成 S:/（最坑，查了一整晚）
- **现象**：`"Setup.exe" /S` 静默安装「成功」（exit 0），但装到了 `D:\study\DeepSeekWhale`
  而不是默认的 `%LOCALAPPDATA%\Programs\`；换目录跑安装器、清环境变量都复现
- **排查**：读遍 electron-builder 的 NSIS 模板（initMultiUser → 注册表 InstallLocation →
  SHGetKnownFolderPath → GetDParameter 扫原始命令行），最后给安装器加 customInit 宏
  把内部变量 dump 到文件（`!macro customInit` + FileOpen/FileWrite，见 build/installer-debug.nsh
  的做法），一眼看到真凶：`CMDLINE=[...exe" S:/]`——**git-bash 的 msys 层把 `/S` 当
  POSIX 路径转换成了 `S:/`**，安装器根本没收到静默参数，弹出的向导是真人点完的
- **连带机制**：assisted 安装器会把 `InstallLocation` 写进注册表
  （include/installer.nsh:104），下次安装优先读它 → 装错过一次就永远装错，
  看起来像「安装器有记忆」，其实只是注册表自延续
- **解决**：从 git-bash 启动安装器时加 `MSYS_NO_PATHCONV=1`（或 `MSYS2_ARG_CONV_EXCL="/S;/D"`）
  关掉参数转换；双机验证时先 `reg delete ...Uninstall\{GUID}` 清掉 InstallLocation 再测
- **坑中坑（bash 自己的反斜杠）**：加了 `MSYS_NO_PATHCONV=1` 后 `/D=D:\study\test` 还是不灵
  ——`\s`/`\t`/`\w` 被吃掉变成 `D:study est`。这次不是 msys，是 **bash 对不带引号单词的
  转义处理**（与 msys 无关，关不掉）。结果：注册表里 `"D:studywhale-test-install\..."`，
  文件被装到盘符相对路径（解析到 D:\ 盘当前目录，即 D:\studywhale-test-install），卸载器
  自己也找不到自己的文件（注册表删了文件全留下，得手动 rm）。**正确姿势：单引号包住
  `/D=` 参数**——`MSYS_NO_PATHCONV=1 "Setup.exe" /S '/D=D:\study\test'`，单引号保留
  反斜杠原样传给 NSIS
- **教训**：①脚本工具调 Windows 安装器，`/S`、`/D=` 这类单字母开关会被 msys 静默改写，
  测试结果全假——先 echo 参数或 dump 安装器内部状态再下结论；②「exit 0 但行为诡异」时
  先怀疑参数在传输中被改，而不是怀疑目标程序；③注册表 InstallLocation 是持久化状态，
  排查安装路径问题时先清它，否则永远在旧状态里打转；④带反斜杠的 Windows 路径参数
  在 bash 里永远单引号包起来，`MSYS_NO_PATHCONV` 只管 msys 转换、管不了 bash 转义

### 坑 #10：NSIS include 被编译两次，卸载器趟里 Var 未引用 → warning 6001 变错误（打包阶段）

- **现象**：加了 `nsis.include` 自定义页（§6.7）后构建失败：
  `warning 6001: Variable "WhaleAutostartCheckbox" not referenced or never set` →
  `Error: warning treated as error`
- **根因**：同一份 include 脚本参与**两趟编译**——安装器趟和卸载器趟（`BUILD_UNINSTALLER`
  已定义）。自定义页的 Var 只在安装器里被使用，卸载器趟里成了「声明了但没人用」，
  electron-builder 的 NSIS 模板把警告当错误，构建直接失败
- **解决**：Var 声明（以及只在安装器存在的任何顶层内容）包一层守卫：
  `!ifndef BUILD_UNINSTALLER ... !endif`。宏（`customPageAfterChangeDir` 等）不用管——
  模板自己只在对应趟里 `!ifmacrodef` 插入
- **教训**：NSIS 多趟编译下，「警告即错误」会把看起来无害的未用变量变成构建炸弹；
  include 里的顶层声明都要想清楚它在哪一趟活着

### 坑 #11：preload 监听依赖 window load，导致鲸鱼偶发全穿透

- **现象**：窗口能显示鲸鱼，但点击、拖动、菜单都没有反应；页面脚本和余额接口正常。
- **根因**：旧版 preload.cjs 把 mousemove、MutationObserver 等监听器全部放在 window.addEventListener(load, ...) 中。透明窗口配合 setIgnoreMouseEvents(true, { forward: true }) 时，个别启动时序下监听没有及时挂上，窗口一直保持穿透。
- **确认方法**：通过 CDP 注入点击可以打开气泡，说明页面事件层正常；查看 preload 日志没有命中检测回调，说明问题在 Electron 鼠标拦截层。
- **修复**：监听器在 preload 顶层立即挂载；命中图改为惰性加载；DOM 观察器兼容 DOMContentLoaded 已触发/未触发两种状态；主进程每 400ms 发送屏幕光标坐标，作为事件链失效时的兜底。
- **验证**：真实鼠标悬停鲸鱼时观察 setIgnore(false)，移开后恢复 setIgnore(true)；再验证点击气泡、拖拽移动和透明区域穿透。
- **教训**：透明窗口的点击穿透是跨主进程、preload、DOM 三层状态机，不能把关键初始化绑在单一生命周期事件上；必须准备独立兜底路径并做真实鼠标回归。
## 5. 常见问题（Q&A）

**Q：为什么鲸鱼默认只在主屏？**
A：窗口尺寸取 `screen.getPrimaryDisplay().workArea`。多屏方案是取所有 display 的并集矩形，
但不同 DPI 的屏幕组合在 Windows 透明窗口上有已知渲染问题，暂不做。

**Q：全屏透明置顶窗口会挡游戏/全屏应用吗？**
A：不会。穿透状态下它不拦截任何事件；独占全屏游戏（exclusive fullscreen）自己接管显示，
普通窗口级全屏照常。任务栏本身是独立的置顶层，也不受影响。

**Q：挂件会不会收键盘输入？**
A：穿透默认开，窗口只在鼠标命中鲸鱼时才收鼠标事件；键盘只在菜单输入框获得焦点时进窗口，
不影响日常打字。

**Q：改 config.json 后要重启吗？**
A：API Key 不用——credentials shim 每次热读文件，点一下鲸鱼立即生效。其余代码改动才需要重启。

**Q：原插件更新了怎么办？**
A：`cd D:\study\DeepSeek-Balance-Whale-Widget && git pull`，重启挂件即生效，桌面端零改动。

**Q：想恢复鲸鱼默认位置/大小？**
A：删 `userdata\`（位置记忆）和 `data\`（大小/菜单设置）两个目录重启。

## 6. 分发：一键安装包（electron-builder + NSIS）

「能跑」之后是「别人能装」。三个改造点，全部围绕一个原则：**安装目录只读，运行数据分离**。

### 6.1 插件打进包里（vendor 化）

开发时直接 import 仓库里的原插件；打包时安装包只有自己的文件，必须把插件拷进来：

```
whale-desktop/
├── vendor/dsh-whale-widget/     ← 原插件仓库的拷贝（lib/ + assets/ + LICENSE）
└── main.js                      ← PLUGIN_DIR 指向 vendor/
```

更新上游：`git pull` 原仓库 → 重新拷贝 → 重新打包。桌面端仍零改写。

### 6.2 运行时路径分流

```js
const IS_PACKAGED = app.isPackaged
const RUNTIME_DIR = IS_PACKAGED
  ? path.join(app.getPath('appData'), 'whale-desktop')  // %APPDATA%：可写
  : APP_DIR                                              // 开发：项目目录
```

config.json / data（DSH_HOME 大小与账本）/ userdata（localStorage 位置记忆）全部跟 RUNTIME_DIR 走。
这样安装目录可以只读，卸载也不丢配置。**改 config 立即生效的机制不受影响**——
credentials shim 热读的就是 RUNTIME_DIR 里的文件。

### 6.3 electron-builder 配置（package.json）

```json
"scripts": { "build": "electron-builder --win" },
"build": {
  "appId": "com.whale.desktop", "productName": "DeepSeekWhale",
  "asar": false,
  "files": ["main.js", "preload.cjs", "vendor/**"],     // 只打这些，node_modules 不需要
  "win": { "target": ["nsis"], "icon": "build/icon.png" },
  "nsis": { "oneClick": false,                            // false = 向导模式（欢迎/选路径/勾选/完成）
            "allowToChangeInstallationDirectory": true,   //   用户可选安装路径
            "runAfterFinish": true,                       // 装完自动启动
            "createDesktopShortcut": true, "shortcutName": "小鲸鱼挂件",
            "installerIcon": "build/icon.ico",            // 向导图标必须 .ico（坑 #8）
            "include": "build/installer-extra.nsh" }      // NSIS 自定义逻辑（开机自启页，§6.7）
}
```

要点：
- `asar:false`：我们动态 `import(PLUGIN_DIR/lib/index.js)`，asar 归档里的文件不能被 Node 直接
  import，关掉最省事（代价是安装目录文件裸露，可接受）
- `oneClick` 两种模式：`true` 双击直接装完（无界面，最省事）；`false` 标准安装向导
  （可选安装路径、勾选快捷方式）。**两种都支持 `/S` 静默安装**（向导模式 /S 走默认值）
- **镜像**：`.npmrc` 追加 `electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/`
  ——构建时还要下载 NSIS/winCodeSign 工具链，同样被墙
- 图标：应用图标 PNG ≥256×256（这里直接拿鲸鱼素材图）；向导图标要 .ico（坑 #8）
- 产物：`dist\DeepSeekWhale Setup 1.1.2.exe`（NSIS 安装向导）+ `dist\win-unpacked\`（免安装绿色版，测试用）

### 6.4 首启引导：填 Key 弹窗

非技术用户不可能去改 JSON 文件，所以做成**首次启动自动弹配置窗**：

```
启动时检查 config.json 无 key → openSetupWindow()（普通窗口，进任务栏）
  └─ 加载 http://127.0.0.1:<port>/setup（我们自己的路由，输出内嵌 SETUP_HTML）
       ├─ 粘贴 key → ipc('save-key') → 主进程写 config.json → 关窗 + 鲸鱼窗口 reload
       └─ 「稍后再说」→ 关窗；托盘新增「配置 API Key…」随时重新打开
```

设计取舍：
- **独立普通窗口，而不是盖在透明鲸鱼页上**——鲸鱼页保持「一行 script」零改写；
  盖上去还得给命中检测加新选择器，透明窗口上做输入框体验也差
- 配置窗单独开 `nodeIntegration:true`（静态本地页 + 需要 ipcRenderer），鲸鱼窗口仍是
  `nodeIntegration:false`，互不影响
- 保存即 `win.webContents.reload()`，配合 credentials shim 热读文件 → 余额立刻刷新，
  与「改 config.json 点一下鲸鱼」同一套机制，殊途同归

### 6.5 静默安装 + SmartScreen

- **静默安装**：`DeepSeekWhale Setup 1.1.2.exe /S`，配 key 可以先写
  `%APPDATA%\whale-desktop\config.json` 再装/再启动，适合批量部署
- **SmartScreen**：个人项目没代码签名证书，别人第一次运行会弹「未知发布者」→ 点「仍要运行」。
  正式分发配证书（或让用户在 Windows Defender 里加白）可消除

### 6.6 打包验证清单（自己先跑一遍）

1. `dist\win-unpacked\DeepSeekWhale.exe` 直接跑：截图自检确认鲸鱼渲染、8 路由注册
2. 安装包 `/S` 静默装：确认装到 `%LOCALAPPDATA%\Programs\DeepSeekWhale\`（坑 #7）
3. 写 key 进 `%APPDATA%\whale-desktop\config.json` → 启动：日志无 NO_KEY、余额显示
4. **无 key 启动** → 配置窗应自动弹出（自检：`WHALE_SETUP_SCREENSHOT` 抓配置窗截图）
5. 注意开发版和安装版的单实例锁不同（userData 目录不同）→ 可能同时出两头鲸，测试时二选一
6. 自定义页（§6.7）：交互装一遍确认开机自启页出现；/S 静默装确认不动 Run 键

### 6.7 自定义页：安装向导里的「开机自启」勾选

用户要求安装时就决定要不要开机自启。electron-builder 的 NSIS 模板在
`assistedInstaller.nsh` 里留了两个插入点：`customWelcomePage`（脚本顶层，可
`!define MUI_PAGE_CUSTOMFUNCTION_SHOW` 占位欢迎页）和 **`customPageAfterChangeDir`**
（选完安装目录之后、开始安装之前——正合适）。用后者插一个 nsDialogs 自定义页：

```
Page custom WhaleAutostartPageCreate WhaleAutostartPageLeave   // 创建/离开回调
  └─ create: nsDialogs::Create 1018 → NSD_CreateCheckbox（默认勾选）→ Show
  └─ leave:  NSD_GetState 存进全局 Var
!macro customInstall
  ${IfNot} ${Silent}          // /S 静默装不弹页也不动 Run 键，保留现状
    checked → WriteRegStr HKCU "...\Run" "DeepSeekWhale" '"$INSTDIR\${APP_FILENAME}.exe" "$INSTDIR"'
    否则   → DeleteRegValue 同键
!macro customUnInstall → DeleteRegValue（防卸载后残留死自启项）
```

关键细节：
- **Run 键格式与 Electron 完全一致**：托盘「开机自启」走 `setLoginItemSettings`，写的
  值名是应用名、值是 `"exe" "目录"`。安装器写同格式 → 两边互相同步，不会各写各的
- **卸载器编译趟的坑（坑 #10）**：同一份 include 会被编译两次（安装器/卸载器），
  自定义页只存在于安装器 → 卸载器趟里 Var 未引用 → warning 6001 → 模板把警告当错误。
  解法：Var 声明包 `!ifndef BUILD_UNINSTALLER`
- 静默安装不弹页，`IfSilent` 跳过写键——否则每次 /S 重装都会把用户手动关掉的自启再打开

### 6.8 紧跟上游：把上游 bug 修进 vendor（位置持久化）

上游 issue #55：拖到非贴角位置重启后弹回角落。根因在 `lib/index.js` 恢复逻辑
把 `hOff/vOff` 清零后调 `settle()`，刚算好的 left/top 被锚点重算覆盖。桌面版同代码、
同 bug。修复三处：

1. `saveConfig()`：存储升 **v:3**，加 `freeH/freeV` 自由轴标记（拖到屏幕中部时
   `state.h/v` 为 null，不记录重启后被强制锚定+镜像）
2. 启动恢复：锚定轴把离边距离带进 `hOff/vOff`（旧代码清零）；自由轴保持
   `h/v = null` 直接用算出的 left/top（settle 自由分支只 clamp 不重算）
3. `applyAnchorPos()`（resize 路径）同样带距离，否则下次 resize 又被 settle 钉回贴角

验证不靠肉眼：开发版 `--remote-debugging-port` 起 CDP，脚本写 localStorage
`dshw-pos` → `Page.reload` → 读 `.dshwv-root` 的 style/class 断言三组用例
（v3 自由位 / v3 锚定位 / v2 旧数据兼容）。流程可复用：**上游 issue 就是需求清单，
修进 vendor 让桌面版先受益，同时整理成 PR 提回上游**（§8 的 P6 提 PR 打法）。

**注意**：vendor 从此与上游不一致——README 差异节写明本地补丁，等上游合并 PR 后
重新同步 vendor 消除分歧。

## 7. 交付物清单

| 文件 | 作用 | 行数 |
|------|------|:----:|
| `main.js` | 主进程：shim ctx + HTTP 服务 + 透明窗口 + 托盘/自启 | ~150 |
| `preload.cjs` | 点击穿透判定：alpha 命中检测 + 状态联动 | ~110 |
| `index.html` | 内嵌在 main.js 的透明页面（一行 script） | 20 |
| `config.json` | API Key + 自启开关（首次运行自动生成） | 5 |
| `package.json` | 含 electron-builder 打包配置（§6.3） | — |
| `.npmrc` | npmmirror 镜像（electron + builder 工具链） | — |
| `build/icon.png` | 安装包/快捷方式图标（鲸鱼素材图） | — |
| `vendor/dsh-whale-widget/` | 原插件拷贝，打进安装包（§6.1） | — |
| `dist/*.exe` | 交付物：NSIS 一键安装包 + win-unpacked 绿色版 | — |
| `README.md` | 使用文档（含一键安装说明） | — |
| `移植教程.md` | 本文档 | — |
| `data/` `userdata/` | 运行时生成：设置/账本/位置记忆 | — |

**复用模板**：以后做别的桌面宠物（网页挂件 → 桌面），把 main.js 里的插件路径、
页面 script 地址、命中区域选择器（preload.cjs 的 `isInteractive`）换成新目标即可，
其余全部通用。
